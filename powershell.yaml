steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline' # 'filePath' | 'inline'. Type. Default: filePath.
    script: |
      $test = "${{ parameters.service}}API"
      $testing = "${{ parameters.service}}API"
      Write-Host $test
      Write-Host "This is my variable $($($test))"
      Write-Host "$(testAPI)"
      Write-Host "This is my variable $(testAPI)"
      Write-Host "This is my variable $($testing)"